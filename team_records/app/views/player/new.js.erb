<% if @player.errors.empty?  %>
  $('.heading').empty();
  $('.heading').append("<h2>Creating new Team</h2>");
  $('.content').empty();
  $('.content').append("<%=raw escape_javascript( render :template => 'player/new.html.erb') -%>");
<% end %>